# Copy to .env.local and adjust if your backend runs on a different port.
# .env.local is git-ignored, so local URLs are never committed.
#
# Default backend started with:  python -m uvicorn app.main:app --reload
VITE_API_BASE_URL=http://127.0.0.1:8000
